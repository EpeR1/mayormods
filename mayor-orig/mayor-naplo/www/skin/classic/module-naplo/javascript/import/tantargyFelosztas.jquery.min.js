
$(function() {
    $('table.ttf thead input[type="checkbox"]').bind('click', function(event) {
	    element = $(event.target);
	    // Az összes elem kiválasztása...
	    cls = element.val();
	    $('table.ttf tr.'+element.val()).toggle();
    });


    $('table.ttf button.nevsorSzinkron_btn').bind('click', function() {
	$('table.ttf tbody input[type="checkbox"].nevsorSzinkronizal').prop( "checked", true );
    });
});
